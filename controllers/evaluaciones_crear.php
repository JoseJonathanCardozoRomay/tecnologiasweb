<?php
require_once __DIR__.'/../includes/verificar_sesion.php';require_once __DIR__.'/../includes/funciones.php';requireRole(['estudiante']);require_once __DIR__.'/../config/conexion.php';require_once __DIR__.'/../models/EvaluacionModel.php';require_once __DIR__.'/../models/EstudianteModel.php';
$m=new EvaluacionModel($pdo);$em=new EstudianteModel($pdo);$errores=[];$mi=esEstudiante()?$em->obtenerPorUsuario((int)$_SESSION['id_usuario']):null;$idEstudiante=$mi?(int)$mi['id_estudiante']:null;$datos=['id_tutoria'=>$_POST['id_tutoria']??$_GET['id_tutoria']??'','calificacion'=>$_POST['calificacion']??'','comentario'=>trim((string)($_POST['comentario']??''))];
if($_SERVER['REQUEST_METHOD']==='POST'){exigirCsrf();$idT=validarId($datos['id_tutoria']);if(!$idT)$errores[]='Selecciona una tutoría válida.';if($idT&&$idEstudiante!==null&&!$m->perteneceAEstudiante($idT,$idEstudiante))$errores[]='La tutoría seleccionada no pertenece a tu cuenta.';if($idT&&!$m->esTutoriaRealizada($idT))$errores[]='Solo puedes evaluar tutorías realizadas.';if($idT&&$m->yaEvaluada($idT))$errores[]='Esta tutoría ya cuenta con una evaluación.';if(!filter_var($datos['calificacion'],FILTER_VALIDATE_INT,['options'=>['min_range'=>1,'max_range'=>5]]))$errores[]='La calificación debe estar entre 1 y 5.';if(!textoValido($datos['comentario'],0,1000))$errores[]='El comentario no puede superar 1000 caracteres.';if(!$errores){try{$m->crear($datos);registrarAccion($pdo,'CREAR','Evaluaciones','Se registró una evaluación.');flash('success','Evaluación registrada correctamente.');redirect('evaluaciones_listar.php');}catch(PDOException $e){$errores[]='No se pudo registrar la evaluación.';}}}
$tutorias=$m->tutoriasDisponibles($idEstudiante);$tituloPagina='Nueva Evaluación - Sistema de Tutorías';require __DIR__.'/../views/evaluaciones/form.php';
